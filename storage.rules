rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /receipts/{userId}/{fileName} {
      // Only the specific user can read, write, or delete their own receipts
      allow read, write, delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
